task: >
  Complete all experiments in docs/requirement0706.md: G0-G3 high-Gc bid
  thresholds and recalibration, M0-M2 market orders plus M3 when triggered,
  and S0-S2 stop-loss/take-profit plus S3 when triggered. Maximize frozen
  B_test sum_pnl relative to the 42.43 limit-order anchor, preserve the
  specified chronological no-leak and one-read B_test protocol, produce every
  required artifact and ledger row, and do not promote deployment changes.
coordinator:
  max_cycles: 30
  max_depth: 3
  ui:
    interaction_mode: auto
executor:
  max_turns: 100
