task: >
  Completely implement and evaluate T0-T9 from docs/trade l2 optimization-0705.md.
  Maximize frozen B_test sum_pnl relative to the 42.43 anchor while preserving
  fixed forced-wrong-fill semantics, chronological no-leak fitting, and all
  required per-experiment reports, predictions, manifests, metrics, and ledger
  rows. Do not promote or modify deployment/live behavior.
coordinator:
  max_cycles: 20
  max_depth: 3
  ui:
    interaction_mode: auto
executor:
  max_turns: 100
